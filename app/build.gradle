apply plugin: 'com.android.application'

android {
    signingConfigs {
        config {
            keyAlias 'anthony'
            keyPassword 'Untouchable365'
            storeFile file('C:/Users/Anthony/Documents/GoogleAssistantLauncher/app/key.jks')
            storePassword 'Untouchable365'
        }
    }
    compileSdkVersion 28
    defaultConfig {
        applicationId "com.loapps.googleassistantlauncher"
        minSdkVersion 15
        targetSdkVersion 28
        versionCode 1
        versionName "1.0"
        signingConfig signingConfigs.config
    }
    buildTypes {
        release {
            minifyEnabled true
            shrinkResources true
            proguardFiles getDefaultProguardFile('proguard-android-optimize.txt'), 'proguard-rules.pro'
        }
    }
    productFlavors {
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
}
